## 1. 工程基础

- [x] 1.1 创建 Java 21、Maven、Spring Boot 工程结构
- [x] 1.2 移除 MySQL JDBC、JPA 默认依赖和数据库测试前置条件，保留 JUnit 5、Mockito、Spring Boot Test
- [x] 1.3 建立基础包结构：incident、evidence、jstack、metrics、pattern、diagnosis、recovery、memory、llm、common，并建立 React 前端目录
- [x] 1.4 定义通用枚举和值对象：严重级别、置信度、风险等级、执行模式、错误响应

## 2. 事件会话与证据

- [x] 2.1 将故障会话领域模型和仓储改为缓存实现，不使用 JPA Entity、表映射或数据库仓储
- [x] 2.2 实现创建故障会话 API 和会话详情查询 API
- [x] 2.3 将证据领域模型和仓储改为缓存实现，不使用持久化表
- [x] 2.4 实现证据上传 API，校验会话存在并关联证据
- [x] 2.5 为会话创建、会话查询、证据上传、缓存未命中和错误路径添加单元测试

## 3. Jstack 分析

- [x] 3.1 实现 jstack 线程块解析，提取线程名称、状态、tid、nid 和堆栈帧
- [x] 3.2 实现线程状态统计和线程名前缀分组
- [x] 3.3 实现死锁文本检测和同锁对象等待线程聚合
- [x] 3.4 实现热点堆栈聚合和 Redis、JDBC、Kafka、HTTP、文件 IO 阻塞关键词识别
- [x] 3.5 为标准 jstack、锁竞争、Redis IO 阻塞和负例样本添加单元测试

## 4. 指标快照

- [x] 4.1 将 JVM、Redis、Kafka、DB 指标快照模型和仓储改为缓存实现
- [x] 4.2 实现指标快照上传或证据归一化入口
- [x] 4.3 实现诊断上下文中的指标读取和缺失指标表达
- [x] 4.4 为 Redis 指标、Kafka 指标、JVM 指标、缺失指标和缓存未命中场景添加单元测试

## 5. 故障模式检测

- [x] 5.1 定义 `FaultPatternDetector` 接口、故障模式枚举和检测结果模型
- [x] 5.2 实现 Redis 连接池耗尽检测器
- [x] 5.3 实现锁竞争检测器
- [x] 5.4 实现 Full GC 压力检测器
- [x] 5.5 实现 Kafka 积压检测器
- [x] 5.6 为每个检测器添加正例和负例测试，确保结果包含置信度和证据 ID

## 6. 诊断报告

- [x] 6.1 实现诊断上下文构建器，整合会话、证据、jstack 分析、指标快照和故障检测结果
- [x] 6.2 定义 `LlmClient`、`LlmRequest`、`LlmResponse` 和 Mock LLM 实现
- [x] 6.3 实现 Prompt 模板管理和诊断报告生成服务
- [x] 6.4 实现 LLM JSON 输出解析、schema 校验、枚举校验和证据 ID 校验
- [x] 6.5 将诊断报告仓储改为缓存实现，并保留诊断触发 API 和报告查询 API
- [x] 6.6 为有效报告、无效 JSON、缺失证据引用、证据不足和缓存报告查询添加单元测试

## 7. 恢复建议与模拟执行

- [x] 7.1 将恢复动作模型和仓储改为缓存实现，不使用持久化表
- [x] 7.2 实现基于诊断报告的恢复建议生成服务
- [x] 7.3 实现恢复动作生成 API
- [x] 7.4 实现模拟执行服务和执行 API，确保所有执行结果标记为 `SIMULATED`
- [x] 7.5 为低风险建议、高风险人工确认、禁止真实执行、恢复验证说明和缓存恢复动作查询添加单元测试

## 8. 结果文档

- [x] 8.1 将复盘卡片模型和仓储改为缓存实现，并将能力定位为当前会话结果文档
- [x] 8.2 实现根据诊断报告和恢复建议生成 Markdown 结果文档
- [x] 8.3 移除按历史标签查询复盘卡片的长期记忆语义，保留按当前会话生成和读取结果文档
- [x] 8.4 为结果文档生成、当前会话读取和缓存未命中场景添加单元测试

## 9. 前端结果输出

- [x] 9.1 在前端保持现有诊断闭环入口，不新增数据库或历史查询入口
- [x] 9.2 在用户点击生成复盘卡片后展示 Markdown 结果文档
- [x] 9.3 为关键结果文档增加复制或下载入口，输出内容来自当前会话响应
- [x] 9.4 为结果文档展示和复制/下载入口添加 React 单元测试

## 10. 端到端 Demo 与验证

- [x] 10.1 准备 Redis 连接池耗尽 + 锁竞争 Demo 输入样例
- [x] 10.2 更新端到端测试：创建事件、上传日志、上传 jstack、上传 Redis 指标、触发诊断、生成恢复建议、模拟执行、生成结果文档
- [x] 10.3 检查所有 API 响应中的关键字段、状态流转、证据链引用和无数据库依赖
- [x] 10.4 运行 `mvn test` 并修复全部失败测试
- [x] 10.5 运行 `npm.cmd test` 并修复前端结果文档相关失败测试
- [x] 10.6 运行 `openspec.cmd status --change "add-thread-doctor-mvp"` 确认变更可进入 apply 阶段
